<!DOCTYPE html>
<html>
<head>
  <title>Colab</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
  
  <div class="navbar">
    <div class="navbar-inner">
      <%= link_to "CoLab", root_url, :class => "brand" %>
      <ul class="nav">
        <li><%= link_to "Groups", groups_url %></li>
        <li><%= link_to "Discussions", discussions_url %></li>
      </ul>
      <ul class="nav pull-right">
        <%= render 'devise/menu/account_links' %>
        <%= render 'devise/menu/login_links' %>
      </ul>
    </div>
  </div>
  
  <% if notice || alert %>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
  <% end %>

  <div class="container">
    <%= yield %>
  </div>

</body>
</html>
